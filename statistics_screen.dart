import 'package:flutter/material.dart';

class StatisticsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Habit Statistics')),
      body: Center(child: Text('Statistics will be displayed here.')),
    );
  }
}
